{
  "$schema": "https://schema.railpack.com",
  "provider": "node",
  "packages": {
    "node": "22"
  },
  "steps": {
    "install": {
      "commands": [
        "npm install --include=optional"
      ]
    }
  },
  "deploy": {
    "startCommand": "npm run start"
  }
}
